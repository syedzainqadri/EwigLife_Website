import 'package:flutter/material.dart';

class Development extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This is Development Page'),
    );
  }
}
